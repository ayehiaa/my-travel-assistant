# Data Model: Hello World Button

No new entities, tables, or data structures are introduced by this feature.

The feature is a pure UI interaction — a button click triggers an in-memory toast notification via the existing `ToastContext`. No persistence, no network calls.
